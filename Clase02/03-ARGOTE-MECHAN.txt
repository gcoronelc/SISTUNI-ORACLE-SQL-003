

/***********8*****/
SELECT DEPTNO,AVG(SAL)  SUELDO_PRO FROM SCOTT.EMP GROUP BY DEPTNO;

SELECT IDDEPARTAMENTO,AVG(SUELDO) SUELDO_PRO  FROM RECURSOS.EMPLEADO  
GROUP BY IDDEPARTAMENTO;




/***9**/

SELECT CUR_ID,SUM(PAG_IMPORTE) FROM EDUCA.PAGO GROUP BY CUR_ID


/******10******/


SELECT DEPTNO,MAX(SAL) MAXIMO,MIN(SAL) MINIMO,AVG(SAL) PROMEDIO  FROM SCOTT.EMP GROUP BY  DEPTNO 


SELECT IDDEPARTAMENTO,MAX(SUELDO) MAXIMO, MIN(SUELDO) MINIMO,AVG(SUELDO) PROMEDIO  
FROM RECURSOS.EMPLEADO GROUP BY IDDEPARTAMENTO

/***11***/
SELECT DEPTNO,COUNT(EMPNO  )  CANTIDAD 
FROM SCOTT.EMP GROUP BY DEPTNO

SELECT IDDEPARTAMENTO,COUNT(IDEMPLEADO) CANTIDAD  
FROM RECURSOS.EMPLEADO  GROUP BY IDDEPARTAMENTO

/*12*/

SELECT DEPTNO,TO_CHAR(HIREDATE,'YYYY'),COUNT(EMPNO)  CANTIDAD 
FROM SCOTT.EMP GROUP BY DEPTNO,TO_CHAR(HIREDATE,'YYYY')


SELECT IDDEPARTAMENTO,TO_CHAR(FECINGRESO,'YYYY'),COUNT(IDEMPLEADO)
FROM RECURSOS.EMPLEADO  GROUP BY IDDEPARTAMENTO,TO_CHAR(FECINGRESO,'YYYY')

/*13*/
SELECT DEPTNO, COUNT(1) TOTAL_EMP, SUM(SAL) PLANILLA, AVG(SAL) PROMEDIO
FROM SCOTT.EMP
GROUP BY DEPTNO


SELECT IDDEPARTAMENTO, COUNT(1) TOTAL_EMP, SUM(SUELDO) PLANILLA, AVG(SUELDO) PROMEDIO
FROM RECURSOS.EMPLEADO
GROUP BY IDDEPARTAMENTO

/*14*/
SELECT CUR_ID, COUNT(*), SUM(MAT_PRECIO)
FROM EDUCA.MATRICULA
GROUP BY CUR_ID